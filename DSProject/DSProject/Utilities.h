#pragma once


enum LaneFlightType
{
	NORMALL,
	VIPL
};

enum EventType {
	BOOK,
	CANCEL,
	PROMOTE
};
enum LaneState {
	FREE,
	MAINTAINENCE,
	OCCUPIED
};
enum FLightState {
	NONE,
	FLYING,
	WAITING,
	LANDING,
	BOARDING

};

